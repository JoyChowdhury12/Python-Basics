# set is a collection of the unordered items
# each elements of the set must be unique & immutable

collection = {1,2,3,2,3,4, "Hello", "Hello", "World"} # set ignores duplicate values

print(collection)

print(type(collection))

print(len(collection))


# creating empty sets

#collection = {} # this will treat as dictionary

collection = set()

print(type(collection))